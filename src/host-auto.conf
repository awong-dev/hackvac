# Wraps the autogenerated makefile for sdkconfig so the CONFIG_TOOLPREFIX
# can be cleared out to force use of the host toolcahin.
-include $(abspath $(BUILD_DIR_BASE)/include/config/auto.conf)
CONFIG_TOOLPREFIX := ""
